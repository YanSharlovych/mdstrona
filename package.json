{
  "name": "aluteco-wordpress-theme",
  "version": "1.0.0",
  "private": true,
  "description": "Development checks for the ALUTECO WordPress block theme.",
  "scripts": {
    "test": "playwright test",
    "test:browser": "playwright test",
    "test:smoke": "powershell -NoProfile -ExecutionPolicy Bypass -File tests/smoke.ps1"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
